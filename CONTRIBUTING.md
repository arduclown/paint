# Руководство для контрибьюторов (Contributing Guidelines)

Добро пожаловать в проект! Чтобы кодовая база оставалась чистой, а интеграция кода проходила гладко, просим придерживаться следующих правил.

## 1. 📁 Структура и зоны ответственности

Весь исходный код находится в директории `src/`, которая разделена по командам. Каждая команда работает строго в своем каталоге.

-   `src/TeamCore/` — зона ответственности команды Маши
-   `src/TeamImport/` — зона ответственности команды Кирилла
-   `src/TeamTools/` — зона ответственности команды Антона
-   `src/TeamUI/` — зона ответственности команды Насти

> ⚠️ **Важно:** Старайтесь не вносить изменения в чужие директории без крайней необходимости. Если вам нужен код из соседней команды, обсудите это с ними лично — возможно, функционал стоит вынести в директорию common.

## 2. 🌿 Ветки (Branches)

В проекте используется одна основная ветка — `master`. 

*   **`master`** — главная ветка. Здесь находится итоговый код. Коммиты напрямую в `master` запрещены.

## 3. 🔄 Процесс внесения кода (Merge Requests)

Все изменения в ветку `master` должны попадать только через **Merge Request (MR)**.

**Пошаговая инструкция:**

1.  **Создайте новую ветку.**
    Ответвитесь от актуальной ветки `master`. Называйте ветки понятно.

2.  **Внесите изменения.**
    Работайте только в своем каталоге (`TeamCore`, `TeamUI` и т.д.) и в связанных с задачей файлах. Пишите понятные комментарии к коммитам.

3.  **Создайте Merge Request в GitLab.**
    Перейдите в раздел **Merge Requests** и нажмите **New merge request**.
    *   **Source branch:** ваша ветка 
    *   **Target branch:** `master`

4.  **Заполните описание MR.**
    Кратко опишите, что было сделано и почему. Если MR решает какую-то задачу (Issue), укажите ее номер.

5.  **Назначьте ревьюера.**
    Выберите как минимум одного коллегу для ревью.

## 4. ✅ Процесс ревью и мержа

*   **Code Review:** Ваш код должен проверить как минимум **один коллега**. Ревьюер смотрит код, оставляет комментарии и ставит отметку "Approved" (аппрув), если всё хорошо.
*   **Исправление замечаний:** Если ревьюер нашел ошибки, исправьте их в **этой же ветке** и сделайте новый коммит. Новый MR создавать не нужно — он обновится автоматически.
*   **Кто имеет право на мерж:** После того как все замечания исправлены и получен как минимум **1 аппрув**, слить ветку в `master` может только **бригадир**. У рядовых разработчиков прав на запись в `master` нет.

## 5. 🚫 Важные правила

*   Не мержите свой MR самостоятельно (даже если есть аппрув), если вы не бригадир.
*   Перед созданием MR всегда обновляйте свою ветку относительно `master`, чтобы избежать конфликтов.
*   Если возникают конфликты при слиянии, разрешите их в своей ветке или обратитесь к бригадиру.

Спасибо за соблюдение правил и хорошей работы!